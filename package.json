{
  "name": "skizzles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/skizzles-*"
  ],
  "scripts": {
    "build": "bun run plugin:build",
    "agents:build": "bun run packages/skizzles-plugin/src/agent-role-package.ts build",
    "agents:check": "bun run packages/skizzles-plugin/src/agent-role-package.ts check",
    "plugin:build": "bun run agents:build && bun run packages/skizzles-plugin/src/plugin-package.ts build",
    "plugin:check": "bun run agents:check && bun run packages/skizzles-plugin/src/plugin-package.ts check",
    "typecheck": "bunx tsc --noEmit && bun run --cwd packages/skizzles-container-lab typecheck",
    "test": "bun test",
    "test:architecture": "bun test tests/architecture/repository-boundaries.test.ts",
    "test:packaging": "bun test packages/skizzles-plugin/test",
    "verify": "bun run typecheck && bun run plugin:check && bun test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
