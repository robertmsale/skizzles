interface:
  display_name: "Fourth Wall"
  short_description: "Orchestrate bounded Codex task trees"
  brand_color: "#2563EB"
  default_prompt: "Use this skill to coordinate a bounded same-root MultiAgentV2 task tree with disjoint ownership, leaf roles, event-driven waits, and proportionate verification."

policy:
  allow_implicit_invocation: true
